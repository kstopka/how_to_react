import * as React from "react";
import { FunctionComponent } from "react";

interface SecureLinkProps {}

const SecureLink: FunctionComponent<SecureLinkProps> = () => {
    return (
        <div>
            <h1>SecureLink</h1>
        </div>
    );
};

export default SecureLink;
